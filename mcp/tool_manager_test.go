package mcp

// func TestToolExecutor_Execute_Echo(t *testing.T) {
// 	// Given: ToolExecutor가 생성되고
// 	executor := NewToolExecutor()

// 	// When: "echo" 타입으로 실행하면
// 	input := map[string]interface{}{"message": "Hello"}
// 	result, err := executor.Execute(context.Background(), "echo", input)

// 	// Then: 에러 없이 입력이 그대로 반환되어야 함
// 	assert.NoError(t, err)
// 	assert.Equal(t, "Hello", result["message"])
// }
